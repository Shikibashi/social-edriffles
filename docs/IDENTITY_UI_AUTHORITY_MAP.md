# Identity UI Authority Map

Identity controls: DID details, handle verification, PDS/migration, sessions, recovery, lockdown. Service controls: AppView/provider switching and service status. Attention controls: feeds and ranking. Association controls: follows/blocks/mutes. Portable Personalization controls: export/import/preferences. The UI must not cross these domains silently.
